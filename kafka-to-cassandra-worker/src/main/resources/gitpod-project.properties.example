kafka.host=localhost:9092

kafka.topic=record-cassandra-leaves-avro
kafka.consumer.group=send-to-cassandra-api-consumer
kafka.schema=true
kafka.schema.registry.url=http://localhost:8081
kafka.initial-offset=latest

# relative path to schema file
# kafka.schema.filepath=../../../../kafka/leaves-record-schema.avsc

# NOTE be sure to change this to your Keyspace in Astra
cassandra.keyspace=customerkeyspace
cassandra.table=leaves
debug-mode=true
# NOTE be sure to change this to your cassandra.api url for exposed port 8000 (something like: https://8000-b2dfa015-856a-4fda-b510-e78aa8783c14.ws-us02.gitpod.io)
api.host=http://localhost:8000

